<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_incentive" model="ir.ui.view">
        <field name="name">account.move.form.incentive</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="invoice_user_id" position="after">
                <field name="incentive_employee_id"
                       invisible="move_type not in ('out_invoice', 'out_refund')"/>
                <field name="incentive_full_payment_date" readonly="1"
                       invisible="move_type not in ('out_invoice', 'out_refund')"/>
            </field>
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='discount']"
                   position="after">
                <field name="incentive_eligible" optional="show" readonly="1"
                       string="Inc. Eligible"/>
            </xpath>
        </field>
    </record>
</odoo>
